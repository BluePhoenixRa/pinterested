<div class="jumbotron">
  <h1>Welcome to Pinterested.</h1>
  <p>A Pinterest clone built with Ruby on Rails</p>
  
        <% if user_signed_in? %>          
          <!-- Do nothing -->
        <% else %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-success" %>
          <%= link_to 'Sign In', new_user_session_path, class: "btn btn-primary" %>
        <% end %>
</div>